.venv/
.DS_Store
.pytest_cache/
.mypy_cache/
.ruff_cache/
__pycache__/
*.egg-info/
build/
dist/
.env

# reports/ is deliberately tracked: the committed report is the prior report the
# next run compares against, and that comparison is what makes a finding
# "unchanged" or "resolved" rather than perpetually "new".
target/
