name: ci
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - id: aws-creds
        uses: aws-actions/configure-aws-credentials-role-chaining@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
