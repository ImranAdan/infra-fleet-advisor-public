apiVersion: apps/v1
kind: Deployment
metadata:
  name: unsafe-defaults
  namespace: applications
spec:
  replicas: 4
  template:
    spec:
      containers:
        - name: app
          image: example.invalid/app:v1
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
