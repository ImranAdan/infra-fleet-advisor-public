apiVersion: apps/v1
kind: Deployment
metadata:
  name: recreate
  namespace: flux-system
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    spec:
      containers:
        - name: controller
          image: example.invalid/controller:v1
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
